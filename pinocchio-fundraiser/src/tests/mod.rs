//! Integration tests for the pinocchio-fundraiser program.
//!
//! Tests use `litesvm` to run against a local validator simulation
//! without requiring a network connection or a running `solana-test-validator`.
